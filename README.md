# new


«адание само по себе простое

в моем случае из-за не знанием фреймворков, ушло много времени на изучение, и то поверхностное
опыт очень полезный и интересный
«анималс€ в первую очередь бекендом, потому не все реализовал на клиентской части, не успел все зучить необходимое

»спользовал Laravel 5

// DataBase
в Migration есть таблица котора€ используетс€

            $table->increments('id');
            $table->string('title', 150);
            $table->string('author', 100);
            $table->integer('year')->nullable();
            $table->string('description', 2000);
            $table->string('image')->default('images/default.png');
            $table->timestamps();
			
в Seeder есть готовые примеры

// Back-end

Ќе успел сделать весь функционал, в частности поиск и сортировку
«наю как это сделать и что это не сложно


// Front-end

Ќе успел сделать сайт одностраничным, принцип пон€л, но не до конца пон€л как его скрестить с laravel
